function extractData (field) {    const filteredSeries = data.series.filter(serie => serie.alias.includes(field))    if (filteredSeries.length === 0) {        return 0    }    data = filteredSeries[0].datapoints    lastDatapoint = data[data.length - 1]    now = new Date()    hourInMS = 3600000    console.log(lastDatapoint[1])    if (lastDatapoint[1] + (hourInMS * 2) < now) {      return 0    } else {      return (lastDatapoint[0])    }} pv_P = (extractData('pv_P'));... $(svgnode).find("#pv_P")[0].textContent = pv_P;... $(svgnode).find("#pv_flow").attr("fill", "#00b189");, this.props.height